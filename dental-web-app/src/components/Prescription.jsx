import React from 'react';
import { PrescriptionPage } from '../pages/PrescriptionPage';

export const Prescription = (props) => {
  return <PrescriptionPage {...props} />;
};

export default Prescription;
